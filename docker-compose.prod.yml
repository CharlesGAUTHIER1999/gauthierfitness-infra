# ════════════════════════════════════════════════════════════════════
#  GauthierFitness — Production Override
#  Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#  ⚠  Ne pas redéfinir les images ici : docker-compose.yml a déjà les bons noms
# ════════════════════════════════════════════════════════════════════

services:

  nginx:
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - backend_storage:/var/www/html/storage:ro
    restart: always

  backend:
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      LOG_CHANNEL: stderr
    restart: always

  frontend:
    restart: always

  db:
    ports: []
    restart: always

  redis:
    ports: []
    restart: always

  queue:
    restart: always

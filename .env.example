# Variables required by the base docker-compose.yml (local isolated stack).
# For staging/prod, see .env.staging.example / .env.prod.example (domains, HTTPS, etc.)

# Image tag to pull from GHCR (e.g. latest, develop, or a short sha)
IMAGE_TAG=latest

# MySQL database
DB_DATABASE=gauthierfitness
DB_USERNAME=gauthierfitness
DB_PASSWORD=
DB_ROOT_PASSWORD=

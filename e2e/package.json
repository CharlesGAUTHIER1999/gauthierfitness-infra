{
  "name": "gauthierfitness-e2e",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "cypress run --browser chrome",
    "test:headed": "cypress run --browser chrome --headed",
    "open": "cypress open"
  },
  "devDependencies": {
    "@testing-library/cypress": "^10.0.0",
    "cypress": "^15.18.1"
  },
  "dependencies": {
    "k6": "^0.0.0"
  }
}
